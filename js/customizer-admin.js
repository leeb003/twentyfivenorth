/**
 * File customizer-admin.js.
 *
 * Theme Customizer enhancements for the customizer itself
 *
 */
(function($) {
	"use strict";

	// Add icon preview to select lists
	/*
	$(document).ready( function() {
		var select = $('select[data-customize-setting-link="test_font_awesome"]').closest('label').find('.selectize-control');
		$(select).find('.output').each( function() {
			alert($this).attr('class');
		});
	});
	*/

} )( jQuery );
